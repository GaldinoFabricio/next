"use client";

export default async function ComponentsCookie() {
   function handleClick() {
      
   }
   return (
      <main>
         <h1>Components</h1>
         <button onClick={handleClick}>Pegar Cookie</button>
      </main>
   );
}
